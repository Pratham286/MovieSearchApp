import React from 'react'
import SearchBar from '../component/SearchBar'

export default function MovieSearch() {
  return (
    <div>
      {/* Movie Search */}
      <SearchBar />
    </div>
  );
};
